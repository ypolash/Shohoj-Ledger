---
name: gsd:new
description: Initialize a new GSD project (runs gsd-init).
---

# Workflow: gsd:new

Initialize a new GSD project.
Create `SPEC.md`, `STATE.md`, `PLAN.md`, `ARCHITECTURE.md` (if they don't exist in the `gsd/` folder) and start the GSD methodology loop.
